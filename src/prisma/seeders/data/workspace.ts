import { IWorkspace } from '../../../interfaces/prisma';

const workspaceData: IWorkspace[] = [
  {
    name: 'BlogsAPI',
    ownerId: 1,
  },
  {
    name: 'Store Manager',
    ownerId: 2,
  },
];

export default workspaceData;
